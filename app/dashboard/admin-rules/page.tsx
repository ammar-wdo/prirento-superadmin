import Heading from '@/components/heading'
import React from 'react'

type Props = {}

const page = (props: Props) => {
  return (
    <div>
    <Heading title='Admin rules' description='Manage admin rules'/>
</div>
  )
}

export default page