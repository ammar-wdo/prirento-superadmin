import Heading from '@/components/heading'
import React from 'react'

type Props = {}

const page = (props: Props) => {
  return (
    <div>
      <Heading title="Category" description="Manage item's categories" />
    </div>
  )
}

export default page